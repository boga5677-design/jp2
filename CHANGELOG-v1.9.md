# NekoNihon v1.9

- GitHub Actions now builds only `assembleDebug`.
- Removed every workflow invocation of `assembleRelease`.
- Artifact contains exactly one file: `NekoNihon.apk`.
- Legacy `com/petlingo` source tree is removed before build.
